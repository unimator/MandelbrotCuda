#version 330 core

smooth in vec2 UV;

uniform sampler2D TextureSampler;
uniform int MaxIterations;

/*
usamplerBuffer buffer terrainData;

layout(std430, binding = 0) buffer terrainData
{
    int data[16][16][16];
};
*/

void main()
{
	gl_FragColor = vec4(texture2D(TextureSampler, UV).rgb, 1.0);
}

